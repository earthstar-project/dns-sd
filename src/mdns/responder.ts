class Responder {
  constructor() {
    // Takes resource records and a multicast interface

    // Probes

    // Resolves conflicts

    // Responds

    // Can send negative responses (NSEC)

    // Can update records

    // Goodbyes
  }
}
